print(5 + 3)
print(10 - 2)
print(4 * 2)
print(64 / 8)

fav_nbr=6
# Python needs to be indicated when mixing different data types.
# Since 'fav_nbr' is integer and as we are appending it to a string, python gets confused.
# so we need to explicitly indicate python to use fav_nbr as string with the help of str() function 
print 'My favorite number is '+str(fav_nbr)
